import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  // 一个swiper的大小
  swiperSize: {
    width: gScreen.screen_width,
    height: (gScreen.screen_height * 360) / 1200,
  },
});

export default styles;
